bool getPath(TreeNode *root, vector<int> &arr, int elem){
    if(root==NULL)  return false;
    
    arr.push_back(root->val);

    if(root->val == elem)   return true;

    if(getPath(root->left, arr, elem) || getPath(root->right, arr, elem))   return true;

    arr.pop_back();
    return false;
}